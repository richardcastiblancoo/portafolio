---
// Hero.astro
---

<header class="header">
  <section class="hero">
    <div class="hero-content">
      <h1>
        <span class="title-line">
          DESARROLLADOR
          <span class="profile-pic">
            <img src="/richi2.webp" alt="Richard C." />
          </span>
          WEB
        </span>
        <span class="title-line anim-text designer-color" data-animated>DISEÑADOR UI — UX</span>
        <span class="title-line anim-text" data-animated>ENTUSIASTA TECH</span>
      </h1>
      
      <p class="subtitle">
        Comprometido con la creación de productos innovadores, funcionales y
        fáciles de usar para ayudar a las personas a convertir sus ideas en
        experiencias digitales.
      </p>

      <div class="cta-buttons">
        <a href="#projects" class="btn primary">Proyectos <span>→</span></a>
        <a href="/cvrichard.pdf(1).pdf" target="_blank" class="btn secondary">
          Descargar CV <span>⬇</span>
        </a>
      </div>
    </div>
  </section>
</header>

<script>
  const initAnimation = () => {
    const elements = document.querySelectorAll('[data-animated]');
    elements.forEach((el) => {
      const text = el.textContent?.trim() || "";
      el.textContent = ""; 
      const fragment = document.createDocumentFragment();
      
      text.split("").forEach((char, i) => {
        const span = document.createElement("span");
        span.textContent = char === " " ? "\u00A0" : char;
        span.className = "letter";
        span.style.setProperty('--d', `${i * 0.05}s`);
        fragment.appendChild(span);
      });
      el.appendChild(fragment);
    });
  };

  initAnimation();
</script>

<style>
  .header {
    background-color: #0a0a1a;
    min-height: 100vh;
    color: white;
    padding: 1rem;
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center; /* Centra el contenido verticalmente */
    justify-content: center;
  }

  .header::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image: linear-gradient(#ffffff0d 1px, transparent 1px),
      linear-gradient(90deg, #ffffff0d 1px, transparent 1px);
    background-size: 30px 30px;
    opacity: 0.3;
    pointer-events: none;
  }

  .hero {
    width: 100%;
    z-index: 5;
    display: flex;
    justify-content: center;
  }

  .hero-content {
    text-align: center;
    max-width: 900px;
    width: 100%;
    padding: 0 1rem;
  }

  h1 {
    font-size: clamp(2.2rem, 8vw, 4.5rem);
    line-height: 1.1;
    margin-bottom: 2rem;
    font-weight: 800;
    letter-spacing: -0.02em;
  }

  .title-line { 
    display: block; 
    margin-bottom: 0.5rem;
  }

  .designer-color {
    color: #6b6bff;
  }

  /* --- ANIMACIÓN LETRA POR LETRA --- */
  .anim-text :global(.letter) {
    display: inline-block;
    opacity: 0;
    transform: translateY(10px);
    filter: blur(5px);
    animation: revealLetter 0.5s var(--d) forwards ease-out;
  }

  @keyframes revealLetter {
    to {
      opacity: 1;
      transform: translateY(0);
      filter: blur(0);
    }
  }

  .subtitle {
    font-size: clamp(1rem, 2vw, 1.2rem);
    color: #a0a0a0;
    margin-bottom: 3rem;
    max-width: 650px;
    margin-inline: auto;
    line-height: 1.6;
  }

  .cta-buttons {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
  }

  .btn {
    padding: 1rem 2.2rem;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .primary { 
    background-color: #6b6bff; 
    color: white; 
    box-shadow: 0 4px 15px rgba(107, 107, 255, 0.3);
  }

  .secondary { 
    border: 2px solid #6b6bff; 
    color: white; 
  }

  .btn:hover { 
    transform: translateY(-5px);
    filter: brightness(1.1);
  }

  .profile-pic {
    display: inline-block;
    width: clamp(50px, 12vw, 80px);
    height: clamp(50px, 12vw, 80px);
    vertical-align: middle;
    margin: 0 10px;
    position: relative;
    top: -2px;
  }

  .profile-pic img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 50%;
    border: 3px solid #6b6bff;
  }

  /* --- AJUSTES MÓVIL --- */
  @media (max-width: 640px) {
    h1 {
      font-size: 2.5rem;
      padding: 0 0.5rem;
    }

    .subtitle {
      font-size: 1rem;
      padding: 0 1rem;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
      gap: 1rem;
    }

    .btn {
      width: 100%;
      max-width: 320px;
      justify-content: center;
    }

    .profile-pic {
        margin: 0 5px;
    }
  }
</style>